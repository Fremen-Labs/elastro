# Test Knowledge Item (KI): RAG AST Routing

This test file formally investigates the deterministic execution time for semantic routing into the `fremen_codebase_rag` context space using purely backticked definitions.

## Deterministic Code Node Target
The agent will attempt to parse this syntax target:
* Primary Target Function Block: `elastro:elastro/cli/commands/rag.py::0`
* Primary Target Module Name: `rag.py`

If parsed correctly by `ASTParser`, this Knowledge Item will automatically register matching graph links in elastic native `functions_defined` and `functions_called` fields.
